name: demo github actions
#on: [push]
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  ubuntu:
    runs-on: 
      - self-hosted
      - label-1
    steps:
      - uses: actions/checkout@v3
      - run: uname -m
      - run: docker -v
      - run: ls
      - run: git pull origin main
      - run: |
             cat test1
             cat test2
  windows:
    runs-on: 
      - self-hosted
      - javaversion
    steps:
      - uses: actions/checkout@v3
      - run: uname -m
      - run: docker -v
      - run : dir
      - run: git pull origin main
      - run : |
              type test3
              type test4
              type test5
